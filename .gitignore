# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Env / secrets
.env
*.session
*.session-journal

# Node / Next.js
frontend/node_modules/
frontend/.next/
frontend/out/

# OS
.DS_Store

# Any env variant except the example template
.env.*
!.env.example

# Xray client config holds a live Reality private key/UUID — deploy-time
# only, created directly on the server; xray-client-config.example.json
# (tracked) documents the shape.
xray-client-config.json
